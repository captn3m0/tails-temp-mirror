amnesia   ALL = NOPASSWD:SETENV: /sbin/capsh \
    --caps=cap_setpcap\,cap_setuid\,cap_setgid+eip --keep=1 \
    --user=tails-persistent-storage --addamb=cap_setuid\,cap_setgid \
    -- -c python3 /usr/local/lib/tps-frontend
